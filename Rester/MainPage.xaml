<Page
    x:Class="Rester.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Rester"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Main, Source={StaticResource Locator}}">
    <Page.Resources>
        <DataTemplate x:Key="ServiceEndpointActionTemplate">
            <Button Content="{Binding Name}" CommandParameter="{Binding Uri}" />
        </DataTemplate>
        <DataTemplate x:Key="ServiceEndpointTemplate">
            <StackPanel Orientation="Vertical" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal" CornerRadius="5" Background="{ThemeResource ContentDialogBorderThemeBrush}" >
                    <SymbolIcon Symbol="{Binding Symbol}" Margin="5" />
                    <TextBlock Text="{Binding Name}" Style="{StaticResource BaseTextBlockStyle}" Margin="5"/>
                </StackPanel>
                <GridView ItemTemplate="{StaticResource ServiceEndpointActionTemplate}" ItemsSource="{Binding Actions}" Margin="5,0,0,-24" VerticalAlignment="Center">
                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>
                </GridView>
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="ServiceConfigurationTemplate">
            <StackPanel Orientation="Vertical" Margin="10,11,1,0" Padding="10" BorderBrush="{ThemeResource SystemControlBackgroundAccentBrush}" BorderThickness="2" CornerRadius="5">
                <TextBlock Text="{Binding Name}" Style="{StaticResource TitleTextBlockStyle}" Foreground="{ThemeResource ContentDialogBorderThemeBrush}" FontWeight="Bold"/>
                <TextBlock Text="{Binding UriRoot}" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="NoWrap" Margin="0,0,0,15"/>
                <ListView ItemTemplate="{StaticResource ServiceEndpointTemplate}" ItemsSource="{Binding Endpoints}" />
            </StackPanel>
        </DataTemplate>
    </Page.Resources>
    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.Content>
                <Grid/>
            </CommandBar.Content>
            <AppBarButton Icon="Edit" Label="Edit"/>
            <AppBarButton Icon="Document" Label="Log"/>
        </CommandBar>
    </Page.BottomAppBar>
    <ListView Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
              ItemTemplate="{StaticResource ServiceConfigurationTemplate}"
              ItemsSource="{Binding ServiceConfigurations}">
        <ListView.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Vertical" />
            </ItemsPanelTemplate>
        </ListView.ItemsPanel>
    </ListView>
</Page>
